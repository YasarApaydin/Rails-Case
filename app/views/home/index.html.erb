<section class="landing-hero">
  <div class="landing-container">
    <div class="landing-left">
      <span class="landing-badge">
        Rails • PostgreSQL • Google Sheets Sync
      </span>

      <h1>
        Keep Your Products<br/>
        <span>Always In Sync</span>
      </h1>

      <p>
        A professional Rails platform that synchronizes
        your <strong>Products database</strong> with
        <strong>Google Sheets</strong> — safely, consistently,
        and transparently.
      </p>

      <div class="landing-actions">
        <% if user_signed_in? %>
          <%= link_to "Go to Products", products_path, class: "btn primary" %>
        <% else %>
          <%= link_to "Get Started", new_user_registration_path, class: "btn primary" %>
          <%= link_to "Login", new_user_session_path, class: "btn outline dark" %>
        <% end %>
      </div>
    </div>

    <div class="landing-right">
      <div class="mock-card">
        <div class="mock-header">products_sync.rb</div>
        <pre>
sync_from_sheet
✓ created: 12
✓ updated: 5
✓ deleted: 1

sync_to_sheet
✓ success
        </pre>
      </div>
    </div>
  </div>
</section>

<section class="features">
  <div class="features-grid">
    <div class="feature-card">
      <h3>Full CRUD</h3>
      <p>Manage products with validations, UUIDs and audit fields.</p>
    </div>

    <div class="feature-card">
      <h3>Google Sheets Sync</h3>
      <p>Manual one-way or two-way synchronization using Google API.</p>
    </div>

    <div class="feature-card">
      <h3>Safe & Idempotent</h3>
      <p>Run sync multiple times without duplication or corruption.</p>
    </div>
  </div>
</section>